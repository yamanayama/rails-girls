<p id="notice"><%= notice %></p>

<h1>🐈catup🐈</h1>

 <%# <table>
<thead>
  #   <tr>
  #     <th>Name</th>
  #     <th>Description</th>
  #     <th>Picture</th>
  #     <th colspan="3"></th>
  #   </tr>
  # </thead>
  <tbody>
  </tbody>
</table>%>
<%= link_to 'New Idea', new_idea_path %>


  <ul class="imgblock">
    <% @ideas.each do |idea| %>
      <%#  MEMO:画像がなかったら下記をスキップする #%>
      <% next unless idea.picture.present? %>
      <li>
        <%= link_to idea do%>
          <%= image_tag(idea.picture_url) if idea.picture.present? %>
          <p><%= idea.name %></p>
        <% end %>
      </li>
    <% end %>
  </ul>
